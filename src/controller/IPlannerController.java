package controller;

/**
 * This interface represents a planner controller in the calendar system.
 */
public interface IPlannerController {
  /**
   * Starts the planner controller by making the view visible.
   */
  void start();
}
